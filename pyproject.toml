[project]
name = "predictors"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.12.0",
    "notebook>=7.5.0",
    "pre-commit>=4.5.0",
    "python-levenshtein>=0.27.3",
    "torch>=2.9.1",
    "transformers>=4.57.3",
]
